from setuptools import setup, find_packages

setup(
    name="yamate",
    version="0.1.0",
    description="material parameter estimation functions",
    license="MIT",
    packages=find_packages()
    )